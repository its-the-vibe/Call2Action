# Environment variables for Call2Action.
# Copy this file to .env and fill in the real values.
# The .env file is gitignored – never commit it to version control.

# Redis authentication password (leave blank if Redis has no password)
REDIS_PASSWORD=your-redis-password-here

# Path to the YAML configuration file (default: config.yaml)
# CONFIG_PATH=/app/config.yaml
